# multiDal — 项目难点与评测方案

## 一、项目难点

### 1.1 双 Embedding 模型的向量空间差异

```
文本路径: query → BGE Embedding (1024d) → Milvus {kb}_text
图片路径: query → Jina CLIP text encoder (1024d) → Milvus {kb}_image
```

BGE 和 Jina CLIP 的语义空间不同，导致：
- **多路分数不可比**：路径A 的 IP 分数和路径B 的 IP 分数分属不同空间，直接合并排序没有意义——必须用 BCE-reranker 做统一精排
- **ImageEmbedder 每张图片 2 条向量**：像素向量 + 描述向量，共存于 `{kb}_image` collection，都参与检索

### 1.2 PDF 质量天花板

MinerU 云端 API 输出是下游所有阶段的输入，质量问题会逐级放大。

| 场景 | MinerU 表现 | 下游影响 |
|---|---|---|
| 扫描件（无文本层） | OCR 质量取决于图片清晰度 | 文本提取错误 → 向量质量崩塌 |
| 混合排版（中英混排、竖排） | 阅读顺序可能错乱 | chunk 语义断裂 |
| 复杂表格（合并单元格、嵌套） | HTML 表格结构可能不完整 | 表格查询失败 |
| 水印/印章/手写批注 | 可能混入正文 | 噪声污染向量库 |
| CAD/设计图稿 | 可能不被识别 | 直接丢失 |
| 加密 PDF | 完全无法打开 | 任务直接失败 |

### 1.3 多模态 Chunking 的上下文断裂

文本、图片、表格之间存在引用关系。固定窗口切 chunk 会割裂图文关系。

```
原文:
  段落A: "Q1营收表现亮眼..."          → chunk_1
  [营收趋势图.png]                    → chunk_2 (image)
  段落B: "从上图可以看出..."          → chunk_3

问题: 用户问"Q1营收"，chunk_3 被检索到，但说的是"上图"，
     而图片 chunk_2 可能因向量距离远未被召回
```

### 1.4 GPU 显存压力（ImageEmbedder 可选）

ImageEmbedder 默认插入，如 CUDA + Jina CLIP 不可用则跳过。

| 模型 | 预估显存 |
|---|---|
| Jina CLIP v2 | ~1.5 GB (FP16) |
| LLM (qwen-plus API 调用) | 0（远端 API） |
| **本地合计** | **~1.5 GB** |

LLM 通过 DashScope API 远端调用，本地无显存压力。

---

## 二、评测方案

### 2.1 评测层次总览

```
第四层: 工程性能（吞吐 / 延迟）
          ↑
第三层: 消融实验（量化每组件贡献）
          ↑
第二层: RAG 端到端（RAGAS 自动化）
          ↑
第一层: 组件评测（Parser / Embedding / Rerank 离线评估）
```

---

### 2.2 第一层：组件评测

#### Parser 质量

**测试集**：20 份标注 PDF，覆盖 5 类——纯文本、混合图文、扫描件、学术论文、财务报告，每类 ≥ 3 份。

| 指标 | 测量方法 | 目标 |
|---|---|---|
| 文本提取完整率 | MinerU 输出字符数 / 标注字符数 | > 95% |
| 表格结构准确率 | 行数 / 列数 / 合并单元格一致率 | > 90% |
| 图片召回率 | 提取图片数 / 标注图片数 | > 95% |

#### Embedding 质量

| 指标 | 测试集 | 测量方法 | 目标 |
|---|---|---|---|
| 文本 Recall@10 | 200 对 (query, 相关段落) | BGE → Milvus → top-10 是否包含目标段落 | > 90% |
| 图片 Recall@5 | 50 对 (文本query, 相关图片) | Jina CLIP → Milvus → top-5 是否包含目标图片 | > 85% |

#### Rerank 增益

| 指标 | 基线 (recall score) | 目标 (BCE-reranker) |
|---|---|---|
| Recall@5 | 实测 | 基线 + 15% |
| MRR | 实测 | 基线 + 20% |

---

### 2.3 第二层：RAG 端到端

使用 **RAGAS** 框架自动化评估。

**测试集**：50 条 `(question, ground_truth_answer, source_doc)` 三元组。

| 指标 | 含义 | 目标 |
|---|---|---|
| Faithfulness | 答案是否完全来自检索到的上下文 | > 0.90 |
| Answer Relevancy | 答案是否紧扣问题 | > 0.85 |
| Context Recall | 检索到的上下文覆盖了 ground truth 所需信息 | > 0.85 |
| Context Precision | 检索结果中相关项排在前面的比例 | > 0.80 |

---

### 2.4 第三层：消融实验

| 实验 | 文本召回 | 图片召回 | Rerank | Query改写 | Recall@5 预期 |
|---|---|---|---|---|---|
| E1 (基线) | ✓ | | | | — |
| E2 | ✓ | ✓ | | | E1 + 10% |
| E3 | ✓ | ✓ | ✓ | | E2 + 18% |
| E4 (全链路) | ✓ | ✓ | ✓ | ✓ | E3 + 8% |

**注意：代码中不存在 BM25 和描述反查路径。**

---

### 2.5 第四层：工程性能

| 指标 | 测量方法 | 目标 |
|---|---|---|
| 文档吞吐 | 100 页 PDF 从上传到入库的总耗时 | < 5 min |
| 查询延迟 P50 | 100 次查询，端到端延迟中位数 | < 2s |
| 查询延迟 P95 | 100 次查询，端到端延迟 P95 | < 5s |
| 并发 QPS | 10 并发查询，统计 QPS | > 5 QPS |
| Kafka 消费延迟 | 消息从 produce 到 consumer 拉取的时间 | < 500ms |

---

## 三、风险优先级

| 优先级 | 风险 | 应对 |
|---|---|---|
| P0 | PDF 解析质量不可控 | Parser 后加自动校验，不达标不进入向量库，标记 exhausted |
| P0 | 双 Embedding 空间不一致 | BCE-reranker 必须保留，不能省略 |
| P1 | 测试集缺失 | 启动后优先用 10-20 份真实文档构建 RAGAS 测试集 |
| P1 | 图文关系断裂 | 利用 MinerU 坐标信息，召回时自动扩展相邻 chunk |
| P2 | ImageEmbedder 可选，降低复杂度 | 默认回退到纯文本检索，无图片多路召回 |